# ============================================================
# Database Configuration
# Update these values for AWS RDS or Azure MySQL
# ============================================================

db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/ecommerce_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.username=root
db.password=root123

# Connection Pool Settings
db.pool.maxSize=20
db.pool.minIdle=5
db.pool.connectionTimeout=30000

# ============================================================
# Stripe Configuration
# Replace with your actual Stripe API keys
# ============================================================
stripe.secret.key=sk_test_your_stripe_secret_key_here
stripe.publishable.key=pk_test_your_stripe_publishable_key_here
